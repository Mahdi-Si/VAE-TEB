# Causal conv-Transformer raw-signal lag-attention VAE-TEB: the production training config.
#
# The model forecasts the next two minutes of RAW normalized FHR (480 samples per anchor) from a
# single shared latent. A target-only prior and a source-conditioned posterior share one decoder,
# invoked twice with nothing else, so the per-step KL between them is the source-conditioned
# coupling readout. What differs from teb_vae/lag_attn_rws is the two history encoders and nothing
# else: a gated causal depthwise convolution stem followed by pre-normalised causal self-attention
# blocks with rotary position encoding, replacing the dilated-conv-plus-LSTM pair.
#
# This file is written out in full rather than inheriting the sibling's, because the two model
# blocks do not share a schema and a merge would leave dead encoder keys the constructor sweep
# silently drops. The cost of that choice is drift, so every key outside the encoder block is
# pinned equal to the sibling's by a test, against a declared allow-list of what may differ. The
# comparison this package exists to make is against that model on the same shards with the same
# objective, optimiser, schedule and seed; every unintended difference is a confound.
#
# Prerequisites:
#   * HDF5 datasets produced by `hdf5_dataset/new_pipeline/create_new_pipeline.py`, which writes
#     `up_st` (43 channels) and `up_ph` (15 channels) as first-class datasets alongside `fhr_st`,
#     `fhr_ph` and the raw `fhr`/`up` signals.
#   * The stats file at `stat_path` regenerated after any dataset rebuild by
#     `hdf5_dataset/calculate_dataset_stats.py`, with a matching `trim_minutes`. The raw `fhr` is
#     the reconstruction target here, so its normalization is load-bearing, not cosmetic.
#
# Run from the repo root:
#   TEB_RUN_STAMP="$(date '+%Y-%m-%d--[%H-%M]')" torchrun --nproc_per_node=7 \
#       -m teb_vae.lag_attn_transformer_rws.trainer \
#       --config teb_vae/lag_attn_transformer_rws/configs/default.yaml

general_config:
  tag: lag_attn_trf_rws_baseline

  seed: 42
  cuda_devices: [0, 1, 2, 3, 4, 5, 6]
  # Adam's per-parameter normalisation makes the step size invariant to the ~480x loss-scale
  # change from the summed raw NLL, so the sibling's stability-tuned value transfers; the clip
  # threshold below does NOT and is scaled instead.
  lr: 0.0003
  # Singular; the experiment driver reads this exact key. Decay is x0.1 per milestone.
  #
  # Deliberately LATER than the feature-target model's [60, 120], and the reason is the beta ramp
  # below. z is the only route to the decoder here, so the source pathway starts at exactly zero KL
  # and can only earn coupling once beta is on -- which is epoch 50. These milestones give 400
  # epochs at full LR with beta at its endpoint. Identical to the model this one is compared
  # against, which is what keeps the two LR-comparable.
  lr_milestone: [400, 800]
  # Linear learning-rate ramp from zero over this many OPTIMIZER STEPS, before the milestone decay.
  # 0 falls back to the framework's epoch-granularity `lr_warmup_epochs` path.
  #
  # Step-granular rather than epoch-granular because a pre-norm attention stack is fragile in
  # exactly the first few hundred updates, which at this dataset size is a fraction of one epoch --
  # an epoch-granular ramp cannot express it. The framework's own LinearLR path additionally
  # rejects start_factor=0.0 and so cannot ramp from zero at all.
  lr_warmup_steps: 2000
  # Epoch cadence of the loss-curve HTML and the diagnostic figure, both rendered by rank 0
  # inside the fit; at 1 a multi-day run pays two PDF renders plus an HTML rewrite every epoch.
  plot_frequency: 5
  epochs: 5000
  accumulate_grad_batches: 1
  batch_size:
    train: 128
    test: 128

  folders_config:
    out_dir_base: /data/deid/isilon/MS_model/new_vae_teb_cross_attention/lag_attn_transformer_rws

model_config:
  VAE_model:
    # --- KL / beta schedule ----------------------------------------------------
    # NLL summed over the 480-sample block and KL summed over d_z are both nats per anchor, so
    # beta = 1 makes reconstruction-plus-KL the exact ELBO of the source-conditioned branch
    # (the unit-weight base term doubles the reconstruction pressure, so 1.0 is a principled
    # start, not a distinguished optimum). The warm-up starts at exactly 0: z is the ONLY
    # route to the decoder, and a nonzero beta before the decoder can use the latent at all is
    # the standard route to posterior collapse.
    beta_schedule:
      kind: linear_warmup
      start: 0.0
      end: 1.0
      warmup_epochs: 50
    # nats per latent dim per step; 0.0 disables the floor. Deliberately zero: a positive floor
    # is a laundering subsidy (KL filled whether or not it is source-driven), and with the zero-KL
    # start plus the beta warm-up, collapse is guarded structurally.
    free_bits: 0.0
    # Weight of the prior scale anchor sum_d 0.5*(exp(lv_p) - 1 - lv_p), the term that keeps the
    # conditional prior's log-variance off its clamp floor: nothing else in the objective
    # penalises a NARROW prior, and the first production run of this architecture pinned 99.2% of
    # prior coordinates on the -5 floor within one epoch (0.118 at epoch 0, 0.812 at epoch 1).
    # Constant, not a schedule -- the collapse completes between epoch 0 and epoch 1, so any
    # warm-up would arrive after the damage.
    #
    # 0.1, not the 1.0e-2 this shipped at first. That value was a guess, and the sibling package's
    # dev-box arms on the committed HIE shard measured it on the wrong side of a threshold: at
    # 1.0e-2 the prior still finished on its floor, delayed 6.7x in optimizer steps and no more,
    # while at 0.1 it held at a floor fraction of 0.046 with kld_active_frac 0.69 against 0.25 in
    # every collapsed arm. A threshold rather than a dial, because the restoring force
    # 0.5*(exp(lv_p) - 1) SATURATES at 0.5*beta_prior while the reconstruction's opposing pressure
    # grows as the decoder sharpens.
    #
    # That measurement is one shard, in-sample, on the OTHER architecture -- the sweep_beta_prior_*
    # arms below bracket this value on both sides and are what confirm or revise it here, on the
    # architecture the collapse was actually measured on.
    beta_prior: 0.1

    # --- Reconstruction likelihood --------------------------------------------
    # 'gaussian_nll' consumes the decoder's learned log-variance heads, keeping every parameter
    # in DDP's expectation set (plain 'ddp'). 'mse' starves them and forces
    # ddp_find_unused_parameters_true; it exists for debugging, not for headline runs.
    likelihood: gaussian_nll

    # Reconstruction weighting: base and full at unit weight, per the objective's definition.
    lambda_full: 1.0
    lambda_base: 1.0

    # --- Backbone widths -------------------------------------------------------
    d_model: 128
    d_z: 48                # the latent must carry the whole FHR predictive state; sweep axis
    horizon: 30            # H = 30 decimated steps = 2 min forecast
    raw_per_step: 16       # R raw samples per horizon token; horizon * R = 480 per anchor
    warmup_period: 30      # first 2 minutes ignored in every loss
    sequence_length: 300
    c_y: 109               # 43 (fhr_st) + 66 (fhr_ph)
    c_u: 58                # 43 (up_st) + 15 (up_ph); must be 15 when use_up_st=false
    use_up_st: true
    dropout: 0.1
    # Dropout for the SOURCE pathway alone -- its adapter/front end, its encoder, and the attended
    # source summary inside the posterior fusion.
    #
    # null = exactly the previous model, and that is NOT one number. The adapter and the encoder
    # always ran at `dropout` above and keep doing so; the dropout on the attended source summary
    # is a site this key INTRODUCED -- there was none there before -- so null leaves that one OFF.
    # Resolving it to `dropout` as well would put p=0.1 inside the posterior of every run that
    # leaves this key unset, invisible in eval mode. Any explicit value applies to all three sites.
    #
    # This exists because the measured failure is specific to the source pathway: over epochs
    # 55-120 of the 125-epoch run the KL held at 1.44 nats and the latent displacement at 0.084,
    # both unchanged, while the HELD-OUT predictive gain fell from +3.25 to -0.70. Same rate, same
    # magnitude, degraded content -- the map from the source to delta_mu is memorising, and a
    # rate-based lever (beta) cannot reach a failure that happens at constant rate.
    #
    # Shipped null, NOT an elevated value, and that is deliberate: no measurement supports any
    # particular rate here, and putting an unvalidated one in the headline run would confound it.
    # The mechanism ships; sweep_source_dropout_0p2.yaml and _0p3.yaml measure the strength.
    source_dropout: null
    decoder_hidden: 128
    # Inherited from a decoder that emitted feature coefficients; the right bounds for a
    # z-scored raw FHR target are unknown until the first run's log-variance distributions
    # are inspected.
    logvar_clamp: [-5.0, 3.0]
    # Latent stabilisers: saturation magnitudes of the tanh-bounded latent heads.
    mu_scale: 5.0
    delta_mu_scale: 3.0
    delta_logvar_scale: 2.0

    # --- Anchor coverage floor -------------------------------------------------
    # Minimum valid fraction of an anchor's 30-step forecast window for the anchor to enter the
    # loss at all: a half-masked anchor's summed NLL covers half a window and its base-minus-full
    # gap reads spuriously small. Revisit against the logged anchor_coverage_frac distribution.
    coverage_floor: 0.9

    # --- How the base branch gets its latent ------------------------------------
    # 'sample' decodes z^p = mu^p + sigma^p * eps (the shared eps); 'mean' decodes at mu^p.
    #
    # 'mean' is shipped because D_0 is the only term in the objective whose gradient DIRECTLY
    # prefers a narrow prior: it decodes a *sample*, sampling noise can only degrade a forecast,
    # so its gradient on the prior log-variance points down without limit and nothing opposes it
    # from below. The 125-epoch anchored run has logvar_prior_floor_frac climbing 0.017 -> 0.496
    # and still rising at epoch 125; the beta_prior anchor slowed that and did not stop it,
    # because the anchor's restoring force saturates at beta_prior/2 per dimension while the
    # reconstruction's opposing pressure grows as the decoder sharpens.
    #
    # Under 'mean' the prior's scale is left to the two terms that want it WIDE: KL(q||p), which
    # pulls the prior out to cover the posterior, and the scale anchor. Note this does NOT make
    # the model an autoencoder -- the posterior branch, which is the one the coupling is read
    # from, is still sampled. What it removes is the pressure that was making the *prior*
    # deterministic.
    #
    # The cost, accepted deliberately: base and full are no longer bitwise identical at init,
    # since the full branch still carries the posterior's noise. The KL is still exactly zero
    # there, because that depends on the two distributions and not on the samples drawn from
    # them. sweep_base_sample.yaml is the ablate-one arm.
    #
    # The second cost, which is about comparability rather than init: under 'sample' the two
    # branches shared one eps, so the sampling noise cancelled in D_base - D_full and the
    # TRAINING-path pred_gap was a common-random-numbers contrast. Under 'mean' only the full
    # branch is sampled, and sampling noise can only raise a branch's NLL, so the training-path
    # gap is biased down by an amount that has nothing to do with the source. Do not compare a
    # training-path pred_gap across this key. The OFFLINE eval is unaffected -- it rebuilds the
    # pairing from the latent distributions and never reads base_decode -- so cross-run pred_gap
    # comparisons belong there.
    base_decode: mean

    # --- How the posterior's log-variance is built -------------------------------
    # 'residual' adds a bounded delta to the PRIOR's pre-bound raw log-variance; 'independent'
    # gives the posterior its own head.
    #
    # This is the second of the two paths that collapse the prior's scale, and the one that
    # survives base_decode: mean. Under 'residual', logvar_post = smooth_bound(raw_logvar_prior +
    # delta), so the full branch's pressure to sharpen sigma_q lands on the PRIOR's own tensor:
    # D_1 drags the prior's log-variance down alongside its own. Measured on the tiny model, the
    # full branch's reconstruction alone puts a gradient of ~8.7e-2 on the prior log-variance head
    # under 'residual' and exactly 0 under 'independent'.
    #
    # With both keys shipped, the prior's log-variance receives gradient from KL(q||p) -- which
    # pulls it UP, to cover the posterior -- and from the beta_prior anchor, and from nothing that
    # wants it narrow.
    #
    # The cost: q == p at init is no longer structural. It is recovered exactly under
    # head_init_calibration (shipped true), which pins the prior's raw log-variance to the same
    # constant the independent head is seeded at, so the KL is still exactly 0 at step 0.
    #
    # With head_init_calibration FALSE the init KL is not small: two independently initialised
    # heads disagree by whatever their init happens to give, measured on the tiny model at 54
    # nats/step mean and 142 nats/step max, against exactly 0 under 'residual' with or without the
    # calibration. That matters because the readout rests on K_t == 0 at init -- unearned
    # divergence on that scale is indistinguishable from coupling in every downstream figure. Any
    # arm that sets head_init_calibration: false therefore inherits a broken zero-KL start from
    # this key and should pin posterior_logvar_mode: residual unless the arm is about that start.
    # sweep_logvar_residual.yaml is the ablate-one arm.
    posterior_logvar_mode: independent

    # --- Causal input reach budget ---------------------------------------------
    # Bound on how far (seconds) an input feature may read into its own FUTURE. The stored
    # features are two-sided wavelet transforms: at null, the value at anchor t carries raw signal
    # from up to 974 s AFTER t, so the target-only branch is partly reading its own answer and
    # every predictive readout taken against it is measured through that leak. A finite budget
    # prunes the channels whose analytic reach exceeds it and delays the survivors; the input
    # adapters then carry the availability projection and the start embedding that make the
    # zero-filled prefix trainable.
    #
    # 120 resolves to 78 of 109 target channels and 29 of 58 source channels, at a maximum delay
    # of exactly 30 steps -- which is warmup_period, the deepest budget the warm-up admits. It is
    # the shipped value because the 1018-epoch baseline and the 125-epoch anchored run were both
    # measured at null, so their D_base is not a target-only forecast: it is a target-only
    # forecast plus up to sixteen minutes of lookahead, and the source has correspondingly less
    # left to add. sweep_reach_null.yaml is the ablate-one arm that recovers the old behaviour.
    causal_reach_budget_s: 120

    # --- Encoders --------------------------------------------------------------
    # The two history encoders, and the whole of what differs from the model this one is compared
    # against. Each stream is: gated causal depthwise conv stem -> pre-normalised causal
    # self-attention blocks with RoPE -> final RMSNorm.
    #
    # Stem reach is 1 + (5-1)*1 + (9-1)*2 = 21 steps = 84 s: local morphology, not context. An
    # empty schedule (both lists []) is the stem-free architecture arm.
    encoder_conv_kernels: [5, 9]
    encoder_conv_dilations: [1, 2]
    # Encoder self-attention heads. UNRELATED to `num_heads` below, which counts lag-attention
    # heads and latent groups; they happen to agree here and nothing may couple them. The derived
    # head width d_model / encoder_num_heads = 32 must be even (rotary position encoding).
    encoder_num_heads: 4
    encoder_d_ff: 256
    # Full causal prefix for the target: the prior must condition on all of Y_{<=t}.
    target_attention_blocks: 4
    # Bounded causal window for the source, so the source state's reach
    # R_U = 21 + 3*(16-1) = 66 steps = 264 s stays SHORTER than the 360 s lag search range. The
    # encoder characterises a local source neighbourhood; the lag attention selects which one.
    source_attention_blocks: 3
    source_attention_window: 16

    # --- Attention -------------------------------------------------------------
    max_lag: 90            # L = 91 (~6 minutes of UP history at 4 s/step)
    num_heads: 4
    d_head: 32
    use_entmax: true       # exact zeros in the lag distribution, not merely small weights
    attention_grad_checkpoint: false
    # 'alibi_decay' seeds a learnable per-head short-lag prior on the attention scores.
    lag_bias_init: alibi_decay
    # Whether the attention query reads the prior log-variance alongside its mean. false = query
    # from mu^p alone; both inputs are target-only, so source purity holds either way.
    query_uses_logvar: false

    # --- Shared horizon decoder core -------------------------------------------
    horizon_depth: 3
    horizon_kernel: 3
    horizon_film: true

    # --- Initialisation policies (zero-parameter) ------------------------------
    # Applied after the generic init; each is a no-op at the constructor default, so an init-off
    # arm reverts the bundle with a config edit and no code change.
    #
    # Break the near-degeneracy of the 30 horizon tokens at init (~0.999 correlated at std 0.02),
    # so per-block FiLM has token-specific structure to modulate. 0.02 = the core's untouched seed.
    horizon_embed_std: 0.8
    # Calibrate BOTH distribution heads onto their trivial starting points at init. One key, two
    # halves, and setting it false reverts both:
    #   * the shared decoder output heads onto mu=0/sigma=1 -- without it the raw-target NLL
    #     starts ~15 nats/sample above that predictor's ~1.4 level;
    #   * the prior head's log-variance onto exactly 0, i.e. sigma_p = 1, which is the scale
    #     anchor's optimum -- without it the prior starts about 3 nats below it and, at
    #     beta_prior: 0.0, reaches its clamp floor 3.3x sooner in optimizer steps.
    # Note the 1018-epoch baseline run predates the second half: it ran with this key true and
    # with the decoder half only, so its start is NOT reproduced by setting the key false.
    head_init_calibration: true
    # LayerNorm gain on the attended source summary in the posterior fusion: sqrt(d_model/d_head)
    # = sqrt(128/32) = 2, so the 32-wide source summary is not out-columned by the 128-wide state.
    a_head_gain: 2.0

  core_model_checkpoint: null


dataset_config:
  # REPOINT THESE at the rebuilt channel-selection datasets (fhr_ph=66, up_ph=15). The paths are
  # deliberately non-existent rather than pointing at old data, so a run fails on a missing file
  # instead of on a width mismatch someone might "fix" by reverting c_y/c_u.
  #
  # `stat_path` must be regenerated from the SAME rebuilt dataset with trim_minutes=1.0. The raw
  # fhr statistics matter here more than for the feature-target model: fhr is the target.
  vae_train_datasets:
    - /data1/fetal-heart-tracing/HDF5_Datasets/REPOINT_ME_new_channel_selection/pre_training_dataset/train_dataset_cs.hdf5
    - /data1/fetal-heart-tracing/HDF5_Datasets/REPOINT_ME_new_channel_selection/pre_training_dataset/train_dataset_no_cs.hdf5
  vae_test_datasets:
    - /data1/fetal-heart-tracing/HDF5_Datasets/REPOINT_ME_new_channel_selection/pre_training_dataset/test_dataset_cs.hdf5
    - /data1/fetal-heart-tracing/HDF5_Datasets/REPOINT_ME_new_channel_selection/pre_training_dataset/test_dataset_no_cs.hdf5

  # Note the key is `stat_path` while the loader parameter is `stats_path`. A typo'd `stats_path:`
  # here yields None and silently disables normalization, so the entry point guards it explicitly.
  stat_path: /data1/fetal-heart-tracing/HDF5_Datasets/REPOINT_ME_new_channel_selection/stats.hdf5

  dataloader_config:
    # 8 per rank: the prod box has 128 cores against 7 ranks (56 persistent workers), so the
    # extra workers still leave headroom; revisit against the profiler's dataloader-next timing.
    num_workers: 8
    prefetch_factor: 4
    # 'fhr' here is REQUIRED: the raw target must arrive z-scored or the Gaussian NLL is
    # meaningless, and the loader raises nothing on its own. The entry point guards it.
    normalize_fields: ['fhr', 'up', 'fhr_st', 'fhr_ph', 'up_st', 'up_ph']
    dataset_kwargs:
      pin_memory: false
      # The model consumes fhr (raw target), the four feature blocks and weight; up and guid feed
      # the diagnostic plots. The cross-channel FHRxUP phase-harmonic block is DELIBERATELY
      # absent: a coefficient that mixes both signals would destroy the target-only /
      # source-conditioned separation the whole design rests on.
      load_fields:
        [
          'fhr',
          'up',
          'fhr_st',
          'fhr_ph',
          'up_ph',
          'up_st',
          'weight',
          'guid',
        ]
      allowed_guids: null
      cs_label: null
      bg_label: null
      epoch_min: -48000
      epoch_max: null
      label: null
      # 0: the FIFO cache stores fully processed samples (~240 KB each, so 10000 held ~2.4 GB
      # PER WORKER), and under the DistributedSampler's per-epoch reshuffle a worker almost
      # never re-sees a cached index -- the RAM bought near-zero hits.
      cache_size: 0
      # Must match the stats file's trim_minutes or normalization is wrong; a mismatch only
      # warns. The whole raw-index geometry (forecast of anchor t starts at 16*(t+1)) assumes
      # this trimmed grid.
      trim_minutes: 1.0


# Advanced configs ========================================================

advanced_config:
  trainer:
    # REQUIRED for the first controlled comparison: mixed precision would need float32 islands
    # around the log-variances, the closed-form KL and the Gaussian NLL reduction, and any of
    # those moving is a difference this comparison would attribute to the encoder.
    precision: "32-true"
    # MEASURED, replacing a provisional 250.0 that was carried over from the model this one is
    # compared against. The 1018-epoch baseline run logged `train/grad_norm` PRE-clip; its
    # recorded percentiles are q50 2775, q90 3613, q95 3876, q99 4681, q99.9 5866, max 7313, and
    # its smallest recorded value, 703, is still nearly three times the old threshold -- so 100%
    # of that run's steps were clipped and the optimizer was doing normalised-gradient descent at
    # roughly a eleventh of its configured learning rate. 5000 is the smallest round value above
    # the q99 percentile: the healthy tail passes and roughly the top one percent still clips,
    # which is what a clip is for.
    #
    # What that sample is: metrics_history.csv records ONE optimizer step per epoch for this
    # metric, not the epoch's aggregate (the metrics collector reads callback_metrics at
    # validation end, before the training epoch is reduced), so the percentiles above are per-STEP
    # percentiles over 1,019 steps thinned from 452,250 -- the right distribution to set a
    # per-step threshold from. `train/grad_clip_frac` is the same one-step-per-epoch sample of the
    # exceedance indicator, so read it as a mean over epochs rather than per row.
    gradient_clip_val: 5000.0
    gradient_clip_algorithm: "norm"
    sync_batchnorm: false     # there is no BatchNorm anywhere in this model
    deterministic: false
    benchmark: true
    # LIVE for this architecture, and shipped off pending a measurement. The LSTM that made the
    # raw-signal base refuse this key outright is gone, and the other blocker once recorded here
    # was never one for the compiled region: only the net's FORWARD is compiled, and the task runs
    # the objective through orig_model, so the data-dependent mask indexing behind kld_active_frac
    # never enters the graph. What is left is a numerical question rather than a mechanical
    # blocker: inductor may reassociate float arithmetic, and `pred_gap` is a difference of order
    # 1e-1 between two block NLLs of order 1e+2. Before adopting a compiled run, set this true on
    # ONE run, compare `pred_gap` on a fixed batch against the eager value, and only then keep it.
    # Refused outright alongside attention_grad_checkpoint, which genuinely does defeat inductor.
    compile: false
    # Cadence of the per-STEP logger flush: each flush is a synchronous MLflow log_batch HTTP
    # call from rank 0 carrying every on_step metric. Epoch aggregates, metrics_history.csv and
    # the checkpoint monitor are epoch-level and unaffected; only the MLflow step-series
    # resolution changes. Matches the gradient-norm hook's sampling stride.
    log_every_n_steps: 25
    # REQUIRED 0: MetricsLoggingCallback has no sanity guard, so a sanity pass would shift every
    # epoch number against MLflow and the checkpoint filenames.
    num_sanity_val_steps: 0
    # Keep true: the data module builds plain loaders on purpose and relies on Lightning to
    # inject the DistributedSampler and call set_epoch each epoch.
    use_distributed_sampler: true
    profiler: simple

  logging:
    console_level: INFO
    file_level: INFO
    json_log: true
    console_diagnose: false
    rotation: "100 MB"
    retention: "14 days"

  # Loss-spike circuit breaker: NON-FINITE GUARD plus a sign-agnostic ADDITIVE spike test.
  # The RELATIVE test stays off.
  #
  # The relative test is `watched > multiplier * max(EMA, ema_floor)`, which assumes a loss
  # bounded below by zero. main_loss here is a learned-variance Gaussian NLL summed over 480 raw
  # samples: it goes negative, and once the EMA is negative the relative test degenerates to
  # "skip every positive batch". So ema_floor sits far above any reachable loss, which switches
  # the relative test off, and additive_margin carries the finite-blow-up detection: skip when
  # watched > EMA + margin, against the RAW EMA, so it works in the negative-loss regime.
  #
  # This exact configuration has already cost this repository a run: a breaker that trains
  # normally for eighty epochs and then skips every batch forever leaves a frozen validation
  # number and nothing else. PROVISIONAL margin -- re-derive from the first real run's observed
  # main_loss fluctuation; until then the non-finite guard is the hard protection.
  spike_breaker:
    enabled: true
    multiplier: 5.0
    ema_decay: 0.02        # EMA smoothing (smaller = longer memory); reported, not acted on
    warmup_batches: 100    # priming window before gating activates
    comparison_metric: main_loss
    # >> any reachable loss: relative detection off. The exponent sign is not decoration --
    # YAML parses `1.0e9` as a string, which validate_config rejects as not a float.
    ema_floor: 1.0e+9
    # Sign-agnostic finite-spike detection: skip when watched > EMA + margin (nats). 0 disables.
    additive_margin: 1.0e+3
    # Escape hatch: after this many CONSECUTIVE skips, force-accept the next finite-loss batch
    # and hard-re-seed the EMA to it. 0 disables the hatch.
    max_consecutive_skips: 25

  tracking:
    mlflow:
      enabled: true
      tracking_uri: "http://localhost:5000"
      # This model's own experiment, run name and variant tag. Inheriting the sibling's would
      # write these runs into its output tree and mix them into its MLflow experiment, which is
      # exactly the confusion the comparison cannot survive.
      experiment_name: seqvae-teb-lag-attn-trf-rws
      run_name: lag_attn_trf_rws_baseline
      artifact_location: null
      # Logs and registers the final eager model at fit end, via the run-logging callback the
      # trainer builder attaches automatically.
      log_model: true
      # Lightning's own per-checkpoint weight artifacts. Off: the ModelCheckpoint directory is
      # the single copy. Deleting this key silently falls back to log_model and double-stores
      # weights.
      log_checkpoints: false
      log_config_artifact: true
      tags:
        project: teb_vae
        stage: dev
        variant: lag_attn_trf_rws

  callbacks:
    early_stopping:
      enabled: false
      monitor: "val/total_loss"
      patience: 300
      min_delta: 0.0001

    model_checkpoint:
      monitor: "val/total_loss"
      save_top_k: 3
      mode: "min"

    # The per-epoch validation diagnostic figure: the raw target in bpm with both forecasts and
    # their uncertainty bands, the prior latent beside the source-derived shift, the per-dimension
    # and total KL, and the two lag panels. The driver imports the plotter only when this is on,
    # so it costs nothing when off.
    #
    # The block name is the INHERITED driver's literal and must stay spelled this way: renaming it
    # to match this package would silently disable the figure with no error anywhere.
    #
    # There is deliberately NO plot_frequency here: the cadence is `general_config.plot_frequency`,
    # the same key that drives loss_plot_epoch.html, so the curves and the figure that explains
    # them always exist for the same epochs.
    lag_attn_rws_plotting:
      enabled: true
      num_examples: 2
      file_format: "pdf"
